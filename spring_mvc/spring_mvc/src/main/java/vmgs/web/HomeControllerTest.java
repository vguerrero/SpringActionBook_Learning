package vmgs.web;

public class HomeControllerTest {
	// @Test
	// public void testHomePage() throws Exception {
	// HomeController controller = new HomeController();
	// MockMvc mockMvc = standaloneSetup(controller).build();
	// mockMvc.perform(get("/")).andExpect(view().name("home"));
	// }



//	@Test
//	public void shouldShowPagedSpittles() throws Exception {
//		List<Person> expectedPersons = createMockPersonList(50);
//		PersonRepository mockRepository = mock(PersonRepository.class);
//		when(mockRepository.findPeople(238900, 50)).thenReturn(expectedPersons);
//
//		PersonController controller = new PersonController(mockRepository);
//		MockMvc mockMvc = standaloneSetup(controller).setSingleView(
//		        new InternalResourceView("/WEB-INF/views/personList.jsp"))
//		        .build();
//		mockMvc.perform(get("/person/list?max=238900&count=50"))
//		        .andExpect(view().name("personList"))
//		        .andExpect(model().attributeExists("personList"));
//		// .andExpect(model().attribute("personList",
//		// hasItems(expectedPersons.toArray()))
//		;
//
//	}
}
